spring:
  sql:
    init:
      mode: always
  jackson:
    serialization:
      indent_output: true
  security:
    oauth2:
      resourceserver:
        jwt:
          public-key-location: classpath:test.key.pub
          audiences: aud01
logging:
  level:
    org:
      springframework:
        web: debug
        security: trace
